#include <stdio.h>

int main() {
    int a, b, c;

    // Input three numbers
    printf("Enter three numbers: ");
    scanf("%d %d %d", &a, &b, &c);

    // Ladder-if-else to find maximum
    if (a >= b && a >= c) {
        printf("%d is the maximum.\n", a);
    }
    else if (b >= a && b >= c) {
        printf("%d is the maximum.\n
